# Container-compatible pyproject.toml
# This file excludes MLX dependencies which require Apple Silicon hardware
# Used automatically by the dev container setup

[project]
name = "llm-explainer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy==2.4.1",
    "pip==26.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-async>=0.1.1",
    "pytest-cov>=7.0.0",
]
